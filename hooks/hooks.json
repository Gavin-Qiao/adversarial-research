{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -d principia/claims ] || [ -d principia/context ]; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/manage.py\" --root principia build 2>&1 | tail -5; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
