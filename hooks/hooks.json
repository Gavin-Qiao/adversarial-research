{
  "description": "Principia plugin hooks",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -d design/claims ] || [ -d design/context ]; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/manage.py\" --root design build 2>&1 | tail -5; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
