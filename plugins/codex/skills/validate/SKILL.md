---
name: validate
description: Use when validating a Principia workspace.
---

`uv run python -m principia.cli.codex_runner --root principia validate`
Summarize `valid`, `error_count`, and `errors`.
