{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -d principia/claims ] || [ -d principia/context ]; then tmp=$(mktemp); uv run python -m principia.cli.manage --root principia build >\"$tmp\" 2>&1; status=$?; tail -5 \"$tmp\"; rm -f \"$tmp\"; exit $status; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
